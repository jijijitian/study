#include<iostream>
#include<algorithm>
using namespace std;

int main()
{
	int n, a[100], k = 0, t;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> t;
		if (t % 2 == 1)
			a[k++] = t;
	}
	sort(a,a + k);
	for (int i = 0; i < k; i++)
		cout << a[i] << " ";
}
