#include<iostream>
using namespace std;

int main()
{
    // From IMUDGESOJ -RID:353639 -PID:1757 -RES:4 -LANG:1

	int a,b;
	cin >> a;
	if(a > 100)
	{
		b = a - (a % 10);
	}
	else
	{
		b = a;
	}
	cout << b;
}
