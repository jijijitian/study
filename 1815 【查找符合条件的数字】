#include<iostream>
using namespace std;

int main()
{
    // From IMUDGESOJ -RID:353653 -PID:1815 -RES:4 -LANG:1

	int a,b;
	cin >> a>>b;
	if (a < b)
	{
		for (int i = a; i <= b; i++)
		{
			if (i % 7 == 0 && i % 10 == 1)
			{
				cout << i << endl;
			}
		}
	}
	else
	{
		for (int i = b; i <= a; i++)
		{
			if (i % 7 == 0 && i % 10 == 1)
			{
				cout << i << endl;
			}
		}
	}
	return 0;
}
