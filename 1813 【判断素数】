#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    // From IMUDGESOJ -RID:361266 -PID:1813 -RES:4 -LANG:1

	int n,b = 1;
	cin >> n;
	for (int i = 2; i <= sqrt(n); i++)
	{
		if (n % i == 0)
			b = 0;
	}
	if (b == 1)
	{
		cout << "YES";
	}
	else
		cout << "NO";

}
